export default (before, after) => {
  // const guild = after.guild;
  //
  // //  If the new role in the cached roles?
  // let diff = after._roles.filter((i) => {
  //   return before._roles.indexOf(i) === -1;
  // });
  //
  // let entryRole = guild.roles.find('name', 'Trainer');
  // let entryChannel = guild.channels.find('name', 'chat');
  //
  // if(diff.indexOf(entryRole.id) !== -1){
  //   let message = `I'd like to introduce to <@${after.user.id}> as the newest ${entryRole.name} of **${guild.name}**!`;
  //   entryChannel.send(message);
  //   console.log(`\n\x1b[42mNew Trainer: ${after.user.username} \x1b[0m\n`);
  // }
}
